import type { SxProps, Theme } from "@mui/material/styles";

export const styles = {
  container: {
    mb: 3,
  } as SxProps<Theme>,

  text: {
    color: "grey.400",
  } as SxProps<Theme>,

  count: {
    color: "white",
    fontWeight: 600,
  } as SxProps<Theme>,
};

